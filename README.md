1. Train large, slow and accurate model (Neural Network)
2. Transfer its knowledge to a smaller, faster, but yet still accurate model (Gradient Boosting Tree) via utilizing the soft labels learned from the teacher model as the target labels for training.
